---
import { getLocaleStaticPaths } from '../_shared/utils';
import BangumiPage from '../bangumi.astro';
export const getStaticPaths = getLocaleStaticPaths;

interface Props {
  locale: string;
}

const { locale } = Astro.props;
---
<BangumiPage locale={locale} />
